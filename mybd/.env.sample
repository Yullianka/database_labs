# Your AWS RDS MySQL endpoint (replace with actual endpoint)
RDS_ENDPOINT=your-mysql-rds-endpoint.xxxxxxxxx.us-east-1.rds.amazonaws.com

# Database credentials (replace with your actual credentials)
DB_USER=admin
DB_PASSWORD=your-mysql-password-here
DB_NAME=new_solar

# Flask secret key for sessions and security (generate a secure random key)
SECRET_KEY=your-super-secret-production-key-here
